[pytest]
testpaths = tests
norecursedirs = data output scripts .git .venv venv .venv
addopts = -q
markers =
    slow: long-running tests (run explicitly)
    gpu: tests requiring GPU/JAX (run explicitly)
