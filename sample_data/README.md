# sample_data

Public synthetic fixtures for reproducibility checks.

- `smoke/`: deterministic release-layer smoke fixture.
- `synthesis/`: deterministic synthesis fixture for ingest/triage/review flows.

These files are public-safe and can be used in CI and local validation.
